apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: drunc-ui-app
  labels:
    app: nginx-svc
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
  selector:
    app: nginx
